import { Router } from "express";
import * as ProductsControlles from "../controllers/products/products.controllers.js"
const router = Router()

// listar productos 
router.post('/create', )
router.get('/get', ProductsControlles.getCategory)

export default router